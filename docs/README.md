# docs/

Supporting documentation for the repository.

| File | Contents |
|------|----------|
| `PROJECT_INVENTORY.md` | Read-through summary of the source workspace this repo was distilled from: the key original scripts and their purpose, the portability issues found (hardcoded paths, absolute `sys.path`), and the refactor performed (config-driven paths, helper package, preserved originals). Useful as a map from the original Svante scripts to the cleaned repo. |

See the top-level [`../README.md`](../README.md) for the workflow overview, external
dependencies, and acknowledgements.
